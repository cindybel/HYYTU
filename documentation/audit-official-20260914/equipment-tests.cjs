const {chromium}=require('C:/Users/VJs DMTeam/AppData/Local/Temp/vj-simulator-qa/node_modules/playwright'),fs=require('node:fs'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const errors=[],checks=[];try{const p=await b.newPage({viewport:{width:1440,height:1000}});p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5181/');await p.locator('#studio-entry-button').click();await p.locator('#vj-name').fill('dmteam');await p.locator('#create-vj-button').click();await p.waitForFunction(()=>player.userData.loaded);
await p.evaluate(()=>StudioWardrobe.open());assert(await p.locator('#physical-panel-back').isVisible());assert.equal(await p.evaluate(()=>PhysicalPanels.current),'wardrobe');await p.screenshot({path:'documentation/audit-official-20260914/wardrobe-audit.png'});await p.locator('#physical-panel-back').click();await p.locator('#studio-world-hud').waitFor({state:'visible'});checks.push('Garde-robe : panneau dédié et retour studio');
await p.evaluate(()=>StudioStorage.open());assert.equal(await p.evaluate(()=>PhysicalPanels.current),'inventory');await p.locator('#physical-panel-back').click();checks.push('Étagère : inventaire partagé et retour studio');
const cases=[];for(const cast of ['nova','eli','sam']){await p.evaluate(id=>{profile.appearance.character=id;applyHumanAppearance()},cast);await p.waitForFunction(id=>player.userData.character===id&&humanLoaded===id,cast);const result=await p.evaluate(()=>{const items=shopItems.filter(i=>i.category==='clothing'),rows=[];profile.equippedWear={};for(const item of items){equipWear(item);applyHumanAppearance();const before=getEquipmentEffects();const snapshot=JSON.stringify(before);equipWear(item);applyHumanAppearance();const after=getEquipmentEffects();const attached=[];player.traverse(o=>{if(o.userData.wearAttachment){const bounds=new THREE.Box3().setFromObject(o),size=bounds.getSize(new THREE.Vector3());attached.push({id:o.userData.itemId,size:size.toArray(),bone:o.parent.name});}});rows.push({id:item.id,stable:snapshot===JSON.stringify(after),attached});}profile.equippedWear={};profile.appearance.headphones=false;applyHumanAppearance();return rows;});for(const item of result){assert(item.stable);assert(item.attached.every(a=>a.size.every(v=>Number.isFinite(v)&&v<3)));}cases.push({cast,items:result});}checks.push('51 combinaisons personnage / article : volumes finis, fixations sur os, bonus non doublés');
for(const id of ['accessory-gaff-pocket','accessory-wide-lens','accessory-mapping-kit','accessory-roadcase-pro','accessory-mini-controller','computer-creator-laptop-rtx','screen-portable-touch']){await p.evaluate(id=>ItemInspector.open(id),id);await p.waitForTimeout(120);await p.screenshot({path:`documentation/audit-official-20260914/model-${id}.png`});await p.locator('[data-model-close]').click();}checks.push('7 nouveaux modèles ouverts et refermés en 3D');assert.deepEqual(errors,[]);fs.writeFileSync('documentation/audit-official-20260914/equipment-tests.json',JSON.stringify({checks,cases,errors},null,2));console.log(JSON.stringify({checks,errors},null,2));}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});

