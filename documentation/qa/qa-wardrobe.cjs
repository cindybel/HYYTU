const {chromium}=require(process.env.VJ_PLAYWRIGHT);const assert=require('node:assert/strict');const path=require('node:path');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5173');await p.locator('#career-reveal-button').click();await p.locator('[data-slot="0"]').click();await p.locator('#slot-confirm-yes').click();await p.waitForFunction(()=>humanLoaded==='nova');const results=[];
for(const id of ['nova','eli','sam']){await p.locator(`[data-human="${id}"]`).click();await p.waitForFunction(id=>humanLoaded===id,id);await p.locator('#shirt-color').selectOption('cyan');await p.locator('#pants-color').selectOption('charcoal');await p.locator('#headphones').check();await p.waitForTimeout(450);const base=await p.evaluate(()=>{const a=[];player.traverse(o=>{if(o.isMesh&&o.material?.name==='wardrobe_top')a.push(o.material.color.getHex());});return a});assert.ok(base.length);await p.screenshot({path:path.join(__dirname,`qa-wear-${id}-cyan.png`)});await p.locator('#shirt-color').selectOption('magenta');await p.waitForTimeout(250);const changed=await p.evaluate(()=>{const a=[];player.traverse(o=>{if(o.isMesh&&o.material?.name==='wardrobe_top')a.push(o.material.color.getHex());});return a});assert.notDeepEqual(base,changed);await p.locator('#headphones').uncheck();assert.equal(await p.evaluate(()=>Boolean(player.getObjectByName('wear-headphones-basic'))),false);
await p.evaluate(()=>{profile.ownedItems.push('clothing-cap-low','clothing-headphones-pro','clothing-vest-led');equipWear(shopItems.find(i=>i.id==='clothing-cap-low'));equipWear(shopItems.find(i=>i.id==='clothing-headphones-pro'));equipWear(shopItems.find(i=>i.id==='clothing-vest-led'));applyHumanAppearance();});await p.waitForTimeout(300);assert.equal(await p.evaluate(()=>Boolean(player.getObjectByName('wear-cap-low')&&player.getObjectByName('wear-headphones-pro')&&player.getObjectByName('wear-vest-led'))),true);await p.screenshot({path:path.join(__dirname,`qa-wear-${id}-accessories.png`)});
 await p.evaluate(()=>{profile.equippedWear={};profile.appearance.headphones=false;applyHumanAppearance();});assert.equal(await p.evaluate(()=>Boolean(player.getObjectByName('wear-headphones-pro'))),false);results.push({id,colorChanges:true,equipRemove:true});}
 await p.evaluate(()=>{profile.created=true;profile.appearance.character='sam';profile.appearance.shirtColor='magenta';slots[activeSlot]=profile;saveSlots();});await p.reload();assert.equal(await p.evaluate(()=>profile.appearance.shirtColor),'magenta');assert.equal(await p.evaluate(()=>profile.appearance.character),'sam');assert.deepEqual(errors,[]);console.log(JSON.stringify({passed:true,results,errors}));await b.close();})().catch(e=>{console.error(e);process.exit(1)});
