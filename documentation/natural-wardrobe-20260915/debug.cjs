const {chromium}=require('../qa-browser.cjs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const p=await b.newPage();p.on('console',m=>{if(m.type()==='error')console.log(m.text())});await p.goto('http://127.0.0.1:5181/');await p.locator('#studio-entry-button').click();await p.locator('#vj-name').fill('Inspect');await p.locator('#create-vj-button').click();await p.waitForFunction(()=>player.userData.loaded);console.log(JSON.stringify(await p.evaluate(()=>{profile.equippedWear={pants:'clothing-pants-tech',top:'clothing-vest-led'};applyHumanAppearance();const e=FittedWardrobe.state.extension,b=e.userData.sourceGarment,sk=b.geometry.attributes.skinIndex,w=b.geometry.attributes.skinWeight;player.updateWorldMatrix(true,true);e.skeleton.update();const sample=[],vv=new THREE.Vector3(),inv=player.matrixWorld.clone().invert();for(let i=0;i<15;i++){vv.fromBufferAttribute(e.geometry.attributes.position,i);e.boneTransform(i,vv);vv.applyMatrix4(e.matrixWorld).applyMatrix4(inv);sample.push({source:[sk.getX(i),sk.getY(i),sk.getZ(i),sk.getW(i)],actual:Array.from(e.geometry.attributes.skinIndex.array.slice(i*4,i*4+4)),w:[w.getX(i),w.getY(i),w.getZ(i),w.getW(i)],rest:Array.from(e.geometry.attributes.position.array.slice(i*3,i*3+3)),posed:vv.toArray()});}return{type:sk.constructor.name,interleaved:sk.isInterleavedBufferAttribute,normalized:sk.normalized,visible:e.visible,bones:e.skeleton.bones.length,scale:e.scale.toArray(),material:e.material.type,draw:e.geometry.drawRange,sample,uniforms:FittedWardrobe.state.uniforms.wardrobeTop.value}})));await b.close()})()
