const {chromium}=require('../qa-browser.cjs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:5181/');await p.locator('#studio-entry-button').click();await p.locator('#vj-name').fill('Inspect');await p.locator('#create-vj-button').click();await p.waitForFunction(()=>player.userData.loaded);for(const cast of ['nova','eli','sam']){await p.evaluate(c=>{profile.appearance.character=c;applyHumanAppearance()},cast);await p.waitForFunction(c=>humanLoaded===c,cast);console.log(JSON.stringify(await p.evaluate(()=>{player.updateWorldMatrix(true,true);const inv=player.matrixWorld.clone().invert(),out=[];player.traverse(o=>{if(o.isSkinnedMesh){o.skeleton.update();const box=new THREE.Box3(),p=new THREE.Vector3(),attr=o.geometry.attributes.position;for(let i=0;i<attr.count;i++){p.fromBufferAttribute(attr,i);o.boneTransform(i,p);p.applyMatrix4(o.matrixWorld).applyMatrix4(inv);box.expandByPoint(p);}out.push({name:o.name,mats:(Array.isArray(o.material)?o.material:[o.material]).map(m=>m.name),count:attr.count,groups:o.geometry.groups,bounds:[box.min.toArray(),box.max.toArray()],matrix:o.matrixWorld.elements,bones:o.skeleton.bones.filter(b=>/Thigh|Calf|Foot/.test(b.name)).map(b=>({name:b.name,pos:b.getWorldPosition(new THREE.Vector3()).applyMatrix4(inv).toArray()}))});}});return{cast:humanLoaded,out};})));}await b.close()})()
