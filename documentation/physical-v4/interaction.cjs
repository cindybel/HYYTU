const {chromium}=require('../qa-browser.cjs'),fs=require('fs'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.stack));await p.goto('http://127.0.0.1:5187/');await p.locator('#studio-entry-button').click();await p.locator('#vj-name').fill('Interactions');await p.locator('#create-vj-button').click();await p.waitForTimeout(500);
 const ids=await p.evaluate(()=>PhysicalV4.state.objects.map(o=>({id:o.uid,model:o.modelId})));const pj=ids.find(o=>o.model==='projector-cheap').id;
 await p.evaluate(id=>{const o=PhysicalCore.get(PhysicalV4.state,id),s=profile.studioWorld;s.x=3.1;s.z=7.5;s.yaw=Math.atan2(s.x-o.position.x,s.z-o.position.z);s.pitch=Math.atan2(o.position.y+.15-1.65,Math.hypot(o.position.x-s.x,o.position.z-s.z));},pj);await p.waitForTimeout(120);
 const xy=await p.evaluate(id=>{const root=PhysicalV4.models.get(id);const v=new THREE.Box3().setFromObject(root.children[0]).getCenter(new THREE.Vector3()).project(camera);return{x:(v.x+1)*innerWidth/2,y:(1-v.y)*innerHeight/2}},pj);await p.mouse.click(xy.x,xy.y);await p.waitForTimeout(80);assert.equal(await p.evaluate(()=>PhysicalV4.selected),pj);await p.keyboard.press('e');await p.waitForTimeout(80);assert.equal(await p.evaluate(()=>PhysicalV4.held),pj);await p.screenshot({path:'documentation/physical-v4/object-in-hand.png'});
 await p.evaluate(()=>{profile.studioWorld.pitch=-.7;profile.studioWorld.yaw=0;});await p.waitForTimeout(80);await p.mouse.click(700,620);await p.waitForTimeout(80);assert.equal(await p.evaluate(()=>PhysicalV4.held),null);const saved=await p.evaluate(id=>{saveSlots();return JSON.stringify(PhysicalCore.get(PhysicalV4.state,id));},pj);
 await p.reload();await p.waitForTimeout(300);assert.equal(await p.evaluate(id=>{PhysicalV4.ensure();return JSON.stringify(PhysicalCore.get(PhysicalV4.state,id));},pj),saved);assert.equal(await p.evaluate(()=>profile.gigs.length),24);assert.equal(await p.evaluate(()=>profile.gigs.every(g=>g.physical)),true);
 assert.deepEqual(errors,[]);console.log('PASS: actual canvas selection, E carry, floor placement, reload retains instance and 24 contracts; no JS errors');fs.writeFileSync('documentation/physical-v4/interaction-result.json',JSON.stringify({passed:true,errors},null,2));}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
